import { describe, it } from '@jest/globals';

describe('whisper function', () => {
  it(
    'should transcribe audio successfully',
    async () => {
      console.log('Implement a test.');
    },
    10 * 60 * 1000,
  );
});
